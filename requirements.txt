# Core data handling
pandas==2.2.3
numpy==1.26.4
scikit-learn==1.5.2

# PyTorch (CPU build used in this project)
torch==2.4.1

# PyTorch Geometric — only SAGEConv + DataLoader are used.
# torch-scatter/sparse/cluster/spline-conv are NOT required for this project; if you
# extend it and need them: pip install torch-scatter torch-sparse \
#   -f https://data.pyg.org/whl/torch-2.4.1+cpu.html
torch-geometric==2.5.3

# Graph building / community detection (Louvain)
networkx==3.3

# Visualization & metrics
matplotlib==3.9.2

# Environment variables (.env -> DATASET_PATH)
python-dotenv

# Optional: only for downloading the UNSW-NB15 dataset (preliminary study)
kagglehub
